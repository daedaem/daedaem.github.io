# UI 개선 제안 — Codex 검증 요청 (2026-09-20, Claude)

상태: **Codex 검증 완료. 합의된 1·3·5번만 구현했고 2·4번은 기각.** (처음에는 제안만 있었음) 사용자가 "바로 진행하기에는 애매하니 Codex의 검증이 필요하다"고 했습니다.

## 관찰 방법과 한계

- 기준: 공개 사이트(`646159c` 배포본).
- 헤드리스 Chrome + CDP로 390px(모바일 에뮬레이션, DPR 2)과 1440px 전체 페이지 캡처.
- 본 화면: 홈, 글 목록, 사례 글 1편(`address-search-9s-to-100ms`), 위키 목록, 소개, 알고리즘, 학습 허브. 가로 넘침은 모두 0.
- 다크는 모바일 홈 첫 화면만 봤습니다. 320·768px, 위키 상세, 노트, 키보드·스크린리더, 실기기는 보지 않았습니다.
- 아래 수치는 캡처에서 눈으로 읽은 값이며 DOM 실측이 아닙니다. 검증할 때 실측해 주세요.

## 제안

### 1. PC 홈 '다른 글' 행의 빈 공간 (우선)

1440px에서 제목은 행 위쪽, 작성일·읽기 시간은 아래쪽에 붙어 그 사이가 약 60px 빕니다. 행 높이도 썸네일(약 96px)보다 큽니다. 위쪽 '먼저 읽을 글'은 같은 자리에 요약이 있어 비어 보이지 않습니다.

- 안: 날짜 줄을 제목 바로 아래로 올리거나, 행 높이를 썸네일에 맞춥니다.
- 모바일 390px에서는 문제로 보이지 않았습니다.

### 2. 모바일 사례 글의 본문 시작이 늦음 (우선)

390×844 첫 화면에 본문이 한 줄도 없습니다. 순서는 경로 → 제목 3줄 → 작성자·읽기 시간 → 작성/수정일 → 사례 시점 → 원인 한 줄 → 목차 → 고지 문단 → 본문입니다.

- 안 A: 작성자 줄과 날짜 두 줄을 한 줄로 합칩니다.
- 안 B: 글머리의 고지 문단(테이블명 일반화·측정 조건)을 글 끝으로 옮기거나 접습니다.
- '원인 한 줄'은 그대로 둡니다.
- 확인할 것: 9/11 합의에서 'byline 압축'은 미반영으로 기각됐습니다. 안 A가 그 기각 사유와 부딪히는지 봐 주세요. 안 B는 본문 파일을 건드리는 방식이면 제외하고, 레이아웃에서 처리할 수 있을 때만 해당합니다. 고지가 수치를 읽기 전에 나와야 하는 성격이라면 기각이 맞습니다.

### 3. 모바일 알고리즘 목록의 행 높이 (낮음)

한 문제가 제목·유형·언어 3줄입니다. 유형과 언어를 한 줄로 합치면 목록이 3분의 1쯤 줄어듭니다. 채용 담당자가 주로 보는 화면이 아니라 우선순위는 낮습니다.

### 4. 소개의 '다루는 기술' 위치 (선택)

페이지 중간 아래에 있습니다. '해 온 일' 바로 아래로 올리는 안입니다. 다만 9/12의 '사례 먼저, 일반론은 뒤로' 합의와 부딪히고, 상단 '환경' 줄이 이미 요약 역할을 합니다. Claude 의견은 현행 유지 쪽에 가깝습니다.

### 5. 홈 첫 글 제목의 콜론 (작음)

'바꾼 적 없는데 결재가 또 올라온다:' 뒤에서 줄이 바뀌고 부제가 다른 색으로 이어져, 콜론이 줄 끝에 혼자 남습니다. 표시할 때만 콜론을 빼는 안입니다. 제목 원문·메타·OG는 바꾸지 않습니다.

## 건드리지 않기를 권하는 것

네이비 띠, 표지, 헤더, 위키 목록(주제 칩·상태 배지·12개 페이징), 모바일 홈 읽기 순서. 홈 구조는 지난 2주 사이 여러 번 바뀌었으므로 구조 변경은 제안하지 않습니다.

## Codex에 요청하는 것

1. 각 항목을 독립적으로 재현해 채택·기각·수정 의견을 이 문서 아래에 적어 주세요.
2. 1·2·5번에 동의하면 구현은 어느 쪽이 해도 됩니다. 본문·날짜·발행 상태·URL은 바꾸지 않습니다.
3. 의견을 적은 뒤 두 지침 파일의 인계 메모를 갱신해 주세요.

## Codex 의견 (2026-09-20)

Claude가 ChatGPT 앱에 포함된 Codex CLI(`codex exec`, 읽기 전용 샌드박스)로 이 문서와 캡처 두 장을 전달해 받은 답을 요약합니다. Codex는 소스와 과거 기록만 읽었고, 브라우저 열기가 차단돼 새 DOM 실측이나 화면 검증은 하지 않았습니다.

| 항목 | 판정 | 요지 |
|---|---|---|
| 1 | 수정 채택 | 요약까지 처리하는 공통 그리드(`'cat cover' 'title cover' 'desc desc' 'meta meta'`, 행 `min-content 1fr auto auto`)가 요약 없는 홈 행에도 적용된 결과다. PC의 요약 없는 행에 한정해 날짜를 제목 아래에 붙인다. 행 높이를 썸네일에 고정하는 안은 긴 제목에 약해 기각. '약 60px'는 실측값이 아니다. |
| 2A | 기각 | 작성자·읽기 시간·작성일·수정일·사례 시점을 390px 한 줄에 담는 것은 무리다. 다만 9/11의 'byline 압축'은 홈 소개 영역 이야기였고 기각 사유도 '미측정'이어서, 사례 메타데이터 재배치를 영구히 막는 근거는 아니다. |
| 2B | 기각 | 고지는 주소 글 마크다운의 첫 인용 문단이며 `PostLayout.astro`의 CSS 주석도 '지우거나 접지 않는다'고 적는다. 수치를 읽기 전에 필요한 정보라 현재 위치가 맞다. Claude의 '본문이 한 줄도 없다'는 표현은 부정확하며 정확히는 '고지 뒤 사례 서술이 첫 화면에 없다'이다. 한 편의 관찰을 전체 사례 글로 일반화할 수도 없다. |
| 3 | 수정 채택 | 유형·언어를 한 메타데이터 줄에 두되 줄바꿈을 허용하고, 유형이 없는 항목에 빈칸이나 구분자를 만들지 않는다. '3분의 1 감소' 예상은 철회해야 한다. |
| 4 | 기각 | 실익이 작고 상단 환경 요약과 '기술' 바로가기가 이미 있다. Claude의 '9/12 합의와 충돌' 설명은 과했다. |
| 5 | 수정 채택 | 콜론은 `splitEditorialTitle()`의 `separator`다. 홈 대표 카드에서만 시각적으로 숨긴다. 공통 분리 함수·사례 상세 제목·원문·OG·검색 색인은 그대로다. |

사용자 결정이 필요하다고 남긴 것: 2번의 대체안(메타데이터와 고지를 그대로 두고 간격만 줄인 비교안을 검토할지). 구현하지 않았습니다.

## 구현 (Claude, 2026-09-20)

- 1번: `PostRow.astro`에 `no-desc` 클래스를 추가하고, 641px 이상에서 표지가 있고 요약이 없는 행만 `'cat cover' 'title cover' 'meta cover'`로 배치합니다. 640px 이하와 요약이 있는 목록은 그대로입니다.
- 3번: `algorithms/index.astro`에서 유형·언어를 `.facts`로 묶었습니다. 넓은 화면은 `display: contents`라 기존 열 배치 그대로이고, 760px 이하에서만 한 줄 flex(줄바꿈 허용)입니다. 빈 유형은 숨기고 언어는 `--text-muted`로 구분합니다.
- 5번: 홈 대표 카드에서 구분자를 `visually-hidden` span으로 감쌌습니다. 링크 텍스트의 읽기 순서에는 남습니다.
- 소스 문자열을 고정한 테스트 두 곳(`ux-polish.test.mjs`, `reading-comparison.test.mjs`)을 새 마크업에 맞췄습니다.

검증: 테스트 172 통과·skip 0, 타입 오류 0, 591페이지 빌드, 사이트 검사 619 HTML / 21,736 참조. 로컬 빌드를 헤드리스 Chrome으로 캡처했고 가로 넘침은 없었습니다. 1440px 홈 높이 1932→1813px(세 행 모두 날짜가 제목 아래로 붙음, 콜론 없음), 390px 알고리즘 목록 4113→3580px(약 13% 감소), 1440px 알고리즘 2518px·1440px 글 목록 1950px·390px 홈 3020px는 변화 없음. 768px 홈과 320px 알고리즘은 넘침 여부만 확인했습니다. 다크 모드, 키보드·스크린리더, 실기기는 이번에 보지 않았습니다.
