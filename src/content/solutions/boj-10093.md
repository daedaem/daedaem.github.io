---
title: '숫자'
site: '백준'
tier: 'Bronze II'
rank: 3
num: '10093'
categories: ['구현']
memory: '24684 KB'
runtime: '232 ms'
problemUrl: 'https://www.acmicpc.net/problem/10093'
langs: ['javascript']
---
```javascript
let [A, B] = require("fs")
  .readFileSync("/dev/stdin")
  .toString()
  .split(" ")
  .map(Number);

if (A > B) [A, B] = [B, A];
// console.log(A, B);
console.log(B - A - 1 < 0 ? 0 : B - A - 1);
let result = [];
for (let i = A + 1; i < B; i++) {
  result.push(i);
}
result = result.join(" ");
console.log(result);
```
