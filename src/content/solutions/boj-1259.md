---
title: '팰린드롬수'
site: '백준'
tier: 'Bronze I'
rank: 4
num: '1259'
categories: ['구현', '문자열']
memory: '9716 KB'
runtime: '124 ms'
problemUrl: 'https://www.acmicpc.net/problem/1259'
langs: ['javascript']
---
```javascript
const FS = require("fs");
const input = FS.readFileSync("/dev/stdin").toString().trim().split("\n").map((el) => el.split(" ").map(Number));

for (let i = 0; i < input.length; i++) {
  let stand = input[i][0];
  if (!stand) break;
  stand = stand.toString().split("");
  let reversed = stand.slice().reverse();
  stand = stand.join("");
  reversed = reversed.join("");
  if (stand == reversed) console.log("yes");
  else console.log("no");

}
```
