---
title: '정수 삼각형'
site: '백준'
tier: 'Silver I'
rank: 9
num: '1932'
categories: ['다이나믹 프로그래밍']
memory: '14136 KB'
runtime: '204 ms'
problemUrl: 'https://www.acmicpc.net/problem/1932'
langs: ['javascript']
---
```javascript
const [[N], ...input] = require("fs")
  .readFileSync(0)
  .toString()
  .split("\n")
  .map((el) => el.split(" ").map(Number));

for (let r = 1; r < N; r++) {
  for (let c = 0; c <= r; c++) {
    const left = c - 1 < 0 ? 0 : input[r - 1][c - 1];
    const right = c == r ? input[r - 1][r - 1] : input[r - 1][c];
    input[r][c] = Math.max(left + input[r][c], right + input[r][c]);
  }
}
console.log(Math.max(...input[N - 1]));
```

> **정리(2026-09-06):** 실행 폴더에 따라 달라지는 상대 경로 대신 표준입력 파일 디스크립터 `0`을 읽도록 바꿨다. 풀이 로직은 바꾸지 않았다.
