---
title: '팰린드롬 만들기'
site: '백준'
tier: 'Silver II'
rank: 8
num: '1254'
categories: ['문자열', '브루트포스 알고리즘']
memory: '9416 KB'
runtime: '124 ms'
problemUrl: 'https://www.acmicpc.net/problem/1254'
langs: ['javascript']
---
```javascript
const INPUT = require("fs").readFileSync("/dev/stdin").toString().trim();

const solve = () => {
  const result = INPUT;
  let rev_result = INPUT.split("").reverse().join("");
  if (rev_result == result) {
    return rev_result.length;
  }
  for (let i = 1; i < result.length; i++) {
    let arr = result.split("").slice(i).join("");
    const reverse_arr = result.split("").slice(i).reverse().join("");
    if (arr === reverse_arr) {
      return result.length + i;
    }
  }
};
console.log(solve());
```
