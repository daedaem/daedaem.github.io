---
title: '햄버거 만들기'
site: '프로그래머스'
tier: 'lv1'
rank: 101
num: '133502'
categories: []
memory: ''
runtime: ''
problemUrl: 'https://school.programmers.co.kr/learn/courses/30/lessons/133502'
langs: ['javascript']
---
```javascript
function solution(ingredient) {
    let answer;
    let stack = [];
    for(let i=0; i< ingredient.length;i++){
        stack.push(ingredient[i]);
        if(stack[stack.length-1]===1 && stack.length >= 3){
            let flag=0;
            for(let idx=1, j=stack.length-1-3; j<=stack.length-1;j++, idx=(idx)%3+1){
                if(stack[j]!=idx) break;
                flag++;
            }
            if(flag===4){
                while(flag--){
                    stack.pop();    
                }
            }
        }
    }
    answer = (ingredient.length-stack.length)/4;
    return answer;
}
```
