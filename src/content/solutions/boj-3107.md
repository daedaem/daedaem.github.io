---
title: 'IPv6'
site: '백준'
tier: 'Silver I'
rank: 9
num: '3107'
categories: ['구현', '문자열']
memory: '9328 KB'
runtime: '120 ms'
problemUrl: 'https://www.acmicpc.net/problem/3107'
langs: ['javascript']
---
```javascript
let Input = require("fs")
  .readFileSync("/dev/stdin")
  .toString()
  .trim()
  .split(":");

let newArr = new Array(8).fill("0000");

const start = Input.findIndex((el) => el == "");

Input = Input.filter((el) => (el == "" ? false : true));
const end = 7 - (Input.length - start);

newArr = newArr.map((el, idx) => {
  if (idx < start || idx > end) {
    let word = Input.shift();
    word = word.split("");
    while (word.length < 4) {
      word.unshift("0");
    }
    return word.join("");
  } else return el;
});

newArr = newArr.join(":");
console.log(newArr);
```
