---
title: '트리의 부모 찾기'
site: '백준'
tier: 'Silver II'
rank: 8
num: '11725'
categories: ['그래프 이론', '그래프 탐색', '트리', '너비 우선 탐색', '깊이 우선 탐색']
memory: '12488 KB'
runtime: '52 ms'
problemUrl: 'https://www.acmicpc.net/problem/11725'
langs: ['cpp']
---
```cpp
#include <iostream>
#include <vector>
using namespace std;
int n;
vector<int> trees[100000+1];
vector<int> parents(100000+1);
void inputData() 
{
	cin >> n;
	for (int i = 1; i <n; i++) 
	{
		int e1, e2;
		cin >> e1 >> e2;
		trees[e1].push_back(e2);
		trees[e2].push_back(e1);
	}
}
void findparent(int start) 
{
	for (int i = 0; i < trees[start].size(); i++) 
	{
		if (parents[trees[start][i]]) continue;
		parents[trees[start][i]] = start;
		findparent(trees[start][i]);
	}
}
void solve() 
{
	parents[1] = 1;
	findparent(1);
	for (int i = 2; i <= n; i++) 
	{
		cout << parents[i] << '\n';
	}
}
int main(void) 
{
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr); cout.tie(nullptr);
	inputData();
	solve();
	return 0;
}
```
