---
title: '수 이어 쓰기 2'
site: '백준'
tier: 'Gold V'
rank: 10
num: '1790'
categories: ['수학', '구현']
memory: '43524 KB'
runtime: '5084 ms'
problemUrl: 'https://www.acmicpc.net/problem/1790'
langs: ['javascript']
---
```javascript
(function solve() {
  const [N, K] = require("fs")
    .readFileSync("dev/stdin")
    .toString()
    .trim()
    .split(" ")
    .map(Number);

  let length = 0;
  for (let i = 1; i <= N; i++) {
    length += i.toString().length;
    if (length >= K) {
      const numStr = i.toString();
      const idx = K - (length - numStr.length);
      console.log(numStr[idx - 1]);
      break;
    }
  }

  if (length < K) {
    console.log("-1");
  }
})();
```
