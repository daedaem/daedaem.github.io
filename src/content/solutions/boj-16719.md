---
title: 'ZOAC'
site: '백준'
tier: 'Gold V'
rank: 10
num: '16719'
categories: ['구현', '문자열', '재귀']
memory: '10460 KB'
runtime: '188 ms'
problemUrl: 'https://www.acmicpc.net/problem/16719'
langs: ['javascript']
---
```javascript
const Input = require("fs")
  .readFileSync("/dev/stdin")
  .toString()
  .trim()
  .split("");

const visit = new Array(Input.length).fill(0);
const solve = (start, end) => {
  if (start == end) return;
  const minLetter = Input.slice(start, end).sort()[0];
  const minIdx = Input.slice(start, end).indexOf(minLetter);
  visit[start + minIdx] = 1;

  let word = "";
  for (let i = 0; i < visit.length; i++) {
    if (visit[i]) {
      word += Input[i];
    }
  }
  console.log(word);
  solve(start + minIdx + 1, end);
  solve(start, start + minIdx);
};

solve(0, Input.length);
```
