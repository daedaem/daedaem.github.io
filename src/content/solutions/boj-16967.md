---
title: '배열 복원하기'
site: '백준'
tier: 'Silver III'
rank: 7
num: '16967'
categories: ['구현']
memory: '26324 KB'
runtime: '288 ms'
problemUrl: 'https://www.acmicpc.net/problem/16967'
langs: ['javascript']
---
```javascript
const [[H, W, X, Y], ...input] = require("fs")
  .readFileSync(0)
  .toString()
  .split("\n")
  .map((el) => el.split(" ").map((el) => Number(el.trim())));
const newArray = JSON.parse(JSON.stringify(input));
for (let r = 0; r < newArray.length; r++) {
  for (let c = 0; c < newArray[r].length; c++) {
    if (r - X < 0 || c - Y < 0) continue;
    newArray[r][c] -= newArray[r - X][c - Y];
  }
}
let result = [];
for (let r = 0; r < H; r++) {
  let temp = [];
  for (let c = 0; c < W; c++) {
    temp.push(newArray[r][c]);
  }
  result.push(temp.join(" "));
}
result = result.join("\n");
console.log(result);
```

> **정리(2026-09-06):** 실행 폴더에 따라 달라지는 상대 경로 대신 표준입력 파일 디스크립터 `0`을 읽도록 바꿨다. 풀이 로직은 바꾸지 않았다.
