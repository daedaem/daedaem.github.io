---
title: '뒤집기'
site: '백준'
tier: 'Silver V'
rank: 5
num: '1439'
categories: ['그리디 알고리즘', '문자열']
memory: '9416 KB'
runtime: '144 ms'
problemUrl: 'https://www.acmicpc.net/problem/1439'
langs: ['javascript']
---
```javascript
const input = require("fs").readFileSync("/dev/stdin").toString().trim();

function solution(input) {
  if (input.includes(0) && input.includes(1)) {
    let toOne = input.split(0).filter((el) => el !== "").length;
    let toZero = input.split(1).filter((el) => el !== "").length;

    return toOne < toZero ? toOne : toZero;
  } else return 0;
}
console.log(solution(input));
```
