---
title: '금액이 마이너스로 찍혔다: VO의 int를 무엇으로 바꿀 것인가'
description: '지원이 끝난 Flash 모듈을 다른 시스템 연동으로 대체하던 중, 검증 단계에서 금액이 음수로 표기되는 현상을 만났다. 원인은 MyBatis 조회 결과를 담는 VO의 필드 타입이었다. 왜 금액에 int를 쓰면 안 되는지.'
date: 2026-08-12
updated: 2026-09-05
happened: '2025년 7월 – 10월'
cause: 'DB 값은 정상이었지만 VO의 int 필드에는 음수가 있었고, 타입을 넓히자 정상화됐다. JDBC 변환과 매핑 이후 연산 중 정확한 발생 지점은 확정하지 않았다.'
category: 'legacy'
tags: ['Java', 'MyBatis', 'VO', '오버플로', 'BigDecimal', '마이그레이션']
draft: false
---

> 코드와 테이블명은 문제의 구조를 보여주기 위해 일반적인 형태로 옮긴 것이다. 실제 시스템의 코드가 아니다.

Flash 기반 모듈이 하나 남아 있었다. Flash는 지원이 끝났고 브라우저에서 실행되지 않으니 손을 대야 했다.

선택지는 두 가지였다. 같은 기능을 다시 만들거나, 이미 같은 데이터를 다루고 있는 다른 시스템과 연동해 대체하거나. 후자를 골랐고, 그 판단과 전환 과정은 [따로 적었다](/posts/retire-flash-module-by-integration/).

전환한 결과를 기존 화면과 대조하는 검증 단계에서, **일부 건의 금액이 음수로 표기됐다.** 실제로는 큰 양수여야 할 값이었다.

## 음수는 어디서 오는가

이 경로에는 뺄셈이 없었다. 큰 양수여야 할 값이 음수로 나온다면, 계산의 결과가 아니라 값이 표현 범위를 넘어간 것을 의심해야 한다. 대부분의 건은 정상이고 금액이 큰 특정 건에서만 부호가 뒤집혔다는 점도 그쪽을 가리켰다.

Java의 `int`는 32비트 부호 있는 정수다. 표현 범위는 다음과 같다.

```
Integer.MIN_VALUE = -2,147,483,648
Integer.MAX_VALUE =  2,147,483,647
```

약 21억이다. 원 단위 금액에서 21억은 큰 수가 아니다. 계약 금액이나 누적 합계라면 어렵지 않게 넘는다.

Java의 일반적인 `int` 덧셈·뺄셈·곱셈은 오버플로를 별도 예외로 알리지 않는다. 결과의 하위 32비트만 남으며, 결과가 반드시 음수가 되는 것은 아니다.

```java
int max = Integer.MAX_VALUE;   //  2147483647
System.out.println(max + 1);   // -2147483648
```

이게 오버플로다. 최상위 비트가 부호 비트인 2의 보수 표현에서, 최댓값에 1을 더하면 최솟값으로 넘어간다. 이 예제에서는 양수가 음수가 되지만, 오버플로 일반을 단순한 부호 반전으로 설명할 수는 없다. JDBC에서 값을 읽는 변환 동작도 이 산술 예제와 별도로 확인해야 한다.

예외가 없다는 점이 이 버그의 성격을 결정한다. 터지지 않으니 로그에도 안 남고, 값이 조용히 틀린 채로 흘러간다. 금액이 음수라서 눈에 띈 것이지, 오버플로가 두 번 일어나 다시 양수가 됐다면 검증에서 걸리지 않았을 수도 있다.

Java에서 `Math.addExact` 같은 메서드는 오버플로 시 `ArithmeticException`을 던진다. 조용히 틀리는 것보다 터지는 편이 나은 계산에는 이쪽을 쓸 수 있다. 다만 이번 건에는 적용하지 않았다.

## VO의 필드 타입

금액이 담기는 자리는 MyBatis 조회 결과를 받는 VO였다. 이런 모양이었다.

```java
// MyBatis 매퍼의 resultType으로 쓰인다
public class ContractVo {
    private String contractNo;
    private int amount;        // 여기
    // ...
}
```

DB 컬럼은 `NUMBER`였다. Oracle의 `NUMBER`는 십진 유효숫자 최대 38자리의 정밀도를 지원한다. 확인한 것은 **DB 값은 정상인데 VO의 `int` 필드에는 음수가 있었다**는 사실이다. 정확한 변환·연산 지점은 아래와 같이 미확정이다.

> 확인한 사실은 세 가지다. DB에 저장된 값은 정상이었다. VO의 `int` 필드에 음수가 담겨 있었다. 그 필드의 타입을 넓히자 정상 값이 나왔다.
>
> 확정하지 않은 것도 있다. 값이 정확히 어느 지점에서 뒤집혔는가다. JDBC 드라이버가 `NUMBER`를 `int`로 읽는 순간인지, 매핑 뒤 Java 코드에서 `int` 연산이나 캐스팅을 거치는 지점인지를 그때 갈라 보지 않았다. JDBC 드라이버 중에는 `int` 범위를 넘는 값을 `getInt`로 읽을 때 numeric overflow 예외를 던지는 것도 있으므로, 예외 없이 음수가 나왔다면 매핑 이후의 연산이나 캐스팅을 먼저 의심하는 편이 맞다. 이건 재현 테스트 하나로 확정할 수 있는 문제이고, 확인되는 대로 여기에 덧붙인다.

기존 Flash 모듈이 이 값을 어떻게 다뤘는지는 확인하지 못했다. 다만 새로 만든 연동 경로에서만 증상이 나왔으므로, 기존 경로는 이 VO를 타지 않았거나 다른 타입으로 받고 있었을 것으로 본다. 이건 추측이다.

## 무엇으로 바꿀 것인가

`long`으로 바꾸면 범위는 약 922경까지 늘어난다. 당장의 오버플로는 사라진다. 하지만 금액을 다루는 타입으로 `long`이 정답인지는 별개 문제다.

정확한 십진 연산이 필요하면 `BigDecimal`이 적합하다. 반면 최소 화폐 단위의 정수로 저장하고 범위와 반올림 규칙을 관리한다면 `long`도 유효한 선택이다. 이번 선택은 후자였다.

### long의 한계

`long`은 정수만 담는다. 원 단위 금액이라면 소수점이 없으니 괜찮아 보이지만, 다음 순간 문제가 된다.

- 세율이나 할인율을 곱하는 계산이 들어올 때
- 외화나 소수점 단위를 다루는 통화가 들어올 때
- 나눗셈이 필요할 때 (`long` 나눗셈은 소수점을 버린다)

### double은 후보가 아니다

부동소수점은 금액에 쓰면 안 된다. 십진 소수를 이진으로 정확히 표현할 수 없기 때문이다.

```java
System.out.println(0.1 + 0.2);        // 0.30000000000000004
System.out.println(0.1 + 0.2 == 0.3); // false
```

합계를 반복해 더하면 오차가 쌓인다. 금액에서 이건 그냥 틀린 값이다.

### BigDecimal

정확한 십진 연산이 필요하면 `BigDecimal`이 맞다.

```java
public class ContractVo {
    private String contractNo;
    private BigDecimal amount;
    // ...
}
```

쓸 때 주의할 것이 두 가지 있다.

**생성자에 double을 넘기지 않는다.** 이미 오차가 낀 값을 그대로 받아 온다.

```java
new BigDecimal(0.1);      // 0.1000000000000000055511151231257827021181583404541015625
new BigDecimal("0.1");    // 0.1
BigDecimal.valueOf(0.1);  // 0.1  (내부에서 Double.toString을 거친다)
```

문자열 생성자나 `valueOf`를 쓴다.

**비교는 `equals`가 아니라 `compareTo`로 한다.** `equals`는 스케일까지 본다.

```java
new BigDecimal("1.0").equals(new BigDecimal("1.00"));      // false
new BigDecimal("1.0").compareTo(new BigDecimal("1.00"));   // 0 (같음)
```

`1.0`과 `1.00`은 값은 같고 스케일이 다르다. 금액 비교에서 `equals`를 쓰면 같은 금액이 다르다고 나온다.

### 실제로 고른 것

이번에는 `long`으로 갔다. **연동 상대가 정수 금액만 받는 계약**과 필요한 표현 범위에 맞춘 선택이었다.

이 금액은 다른 시스템과 주고받는 값이고, 그쪽은 정수 금액만 받는다. 소수점이 붙은 값이 넘어가면 그대로 오류다. 그래서 이 시스템의 금액 계산은 처음부터 정수로 나누어떨어지도록 되어 있었고, 소수가 생길 수 있는 타입을 쓰면 "생기지 않도록 검증하는" 코드가 따라와야 했다.

`long`을 쓰면 소수가 타입 수준에서 존재할 수 없다. 검증으로 막는 것이 아니라 구조로 막는다. 상한도 21억에서 922경으로 올라가 이번 오버플로는 사라진다.

`BigDecimal`로도 스케일과 정수 변환을 검증하면 같은 계약을 지킬 수 있으므로 틀린 선택인 것은 아니다. 이번에는 정수 필드의 단순함을 택했다. 소수 계산이 추가되면 중간 계산의 타입과 반올림 시점을 다시 정해야 한다. `long`이라고 해서 나눗셈의 절삭이나 오버플로까지 막히지는 않는다.

한 가지는 그대로다. 이 VO를 처음 만들 당시에는 `int`로 충분했을 것이고, "지금은 안 넘는다"는 조건은 데이터가 쌓이면서 깨졌다. `long`도 같은 종류의 조건 위에 있다. 922경은 원 단위 금액에서 사실상 넘지 않는 값이라 받아들였을 뿐이다.

## 왜 검증에서야 발견됐는가

기존 화면과 새 화면을 나란히 놓고 값을 대조하지 않았다면 이 버그는 운영에 나갔을 것이다. 예외가 없으니 로그에 안 남고, 금액 하나가 틀린 것은 시스템 어디도 알려 주지 않는다.

레거시를 대체할 때 새 구현이 기능적으로 동작하는지를 확인하는 것만으로는 부족하다. **같은 입력에 같은 출력이 나오는지**를 데이터로 대조해야 한다. 새로 만든 쪽이 옳다는 보장이 없기 때문이다.

## 정리

- 뺄셈이 없는데 결과가 음수라면 오버플로를 먼저 의심한다. `int` 산술은 예외 없이 하위 32비트만 남기고, 상한 21억은 원 단위 금액에서 쉽게 넘는다.
- 금액에 `double`은 쓰지 않는다. 일반적으로는 `BigDecimal`이고, 연동 상대가 정수만 받는다면 `long`으로 소수를 타입 수준에서 막는 편이 단순하다.
- VO의 필드 타입은 DB 컬럼과 연동 상대의 표현 범위에 맞춰 정한다. "지금은 안 넘는다"는 조건은 데이터가 쌓이면 깨진다.
- 레거시 대체는 기능 동작 확인이 아니라 기존 결과와의 값 대조로 검증한다.
